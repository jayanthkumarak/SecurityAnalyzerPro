{
  "name": "forensic-analyzer-pro",
  "version": "2.0.0",
  "description": "Multi-LLM digital forensics platform with OpenRouter integration",
  "private": true,
  "author": {
    "name": "Jayanth Kumar",
    "email": "jayanthkumarak@gmail.com",
    "url": "https://github.com/jayanthkumarak"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jayanthkumarak/ForensicAnalyzerPro.git"
  },
  "bugs": {
    "url": "https://github.com/jayanthkumarak/ForensicAnalyzerPro/issues"
  },
  "keywords": [
    "forensics",
    "security",
    "ai",
    "web-application",
    "threat-intelligence",
    "incident-response",
    "digital-forensics"
  ],
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "dev": "concurrently -k -n API,WEB -c green,cyan \"bun run --cwd api dev\" \"bun run --cwd frontend dev\"",
    "build": "bun run --cwd api build && bun run --cwd frontend build",
    "preview": "concurrently -k -n API,WEB -c green,cyan \"bun run --cwd api start\" \"bun run --cwd frontend preview\"",
    "lint": "eslint \"{api,frontend}/**/*.{ts,tsx,svelte}\"",
    "test": "bun test",
    "clean": "rm -rf api/dist frontend/dist"
  },
  "workspaces": ["api", "frontend"]
}
